# ARIA runtime dependencies (pinned for reproducible server installs).
# Install: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
anthropic==0.109.1
httpx==0.28.1
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
