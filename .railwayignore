# What NOT to upload to Railway. The agent service only needs aria/ + requirements.
# (Excludes the venv, git, dashboard build/deps, DBs, logs, and — critically — .env,
# whose secrets are set as Railway variables instead.)
.venv/
.git/
node_modules/
dashboard/
docs/
tests/
deploy/
scripts/
.brand-preview/
__pycache__/
**/__pycache__/
*.pyc
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
*.log
.env
.DS_Store
